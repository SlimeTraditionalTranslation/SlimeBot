name: ${project.name}
description: ${project.description}

main: tsp.slimebot.SlimeBot
version: ${project.version}
author: Silent
depend: [Slimefun]

commands:
  slimebot:
    usage: /slimebot
    description: Plugin Information